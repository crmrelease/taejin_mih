# Environment variables - NEVER commit
.env
.env.local
.env.*.local

# Per-instance local Claude config (personality, scratch notes) — NEVER commit
CLAUDE.local.md

# Per-instance bot state (Slack thread → Claude session map)
bot/sessions.json

# JSONBin monitor state (seen ids, poll counters)
bot/monitor_state.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build outputs
dist/
build/
*.egg-info/
